# UX Audit Report -- 2026-03-28

Scope: all `client/src/**/*.tsx` files (58 total).
Rules checked: clickable elements, mobile adaptation, loading/empty states, form UX.

---

## Notation

- Pass = no issues found for the given rule
- Fail = actionable issue

---

## Infrastructure / UI primitives (no issues)

The shadcn `Button` component (`components/ui/button.tsx`) already includes
`disabled:pointer-events-none disabled:opacity-50`, so every `<Button disabled>` is covered.
Native `<button>` elements do **not** get this automatically and are checked individually below.

The remaining `components/ui/*.tsx` files (badge, card, dialog, input, scroll-area, select,
separator, sheet, switch, table, textarea) are headless/radix wrappers with no clickable or
loading concerns of their own. They are excluded from the per-file audit.

---

## `App.tsx`

- Routing shell only, no interactive elements.
- No audit items applicable.

---

## `main.tsx`

- Bootstrap file. No audit items applicable.

---

## `pages/customer/ScanPage.tsx`

### Clickable elements
- **line 72-83** `<button>` (Continue) -- no `cursor-pointer` class.
- **line 84-89** `<button>` (Skip) -- no `cursor-pointer` class.
- **line 62-66** `<input>` onKeyDown triggers navigation -- acceptable.
- **line 106-109** `<Button>` (Retry) -- uses shadcn Button, OK.

### Mobile adaptation
- Uses `max-w-xs` for inputs. Adequate for mobile.
- No `sm:`/`md:` breakpoints needed (single-column layout).

### Loading / Empty states
- Loading spinner present (lines 112-127).
- Error state with retry button present (lines 99-110).
- Disabled table state present (lines 91-98).

### Forms
- Input has `placeholder` (line 66).
- No submit button loading state (Continue button has no disabled/loading guard during navigation).

#### Summary
- **line 72-83** `<button>` missing `cursor-pointer`
- **line 84-89** `<button>` missing `cursor-pointer`
- Continue button has no loading/disabled guard during navigation

---

## `pages/customer/LangSelectPage.tsx`

### Clickable elements
- **line 37-45** `<button>` Chinese -- has hover styles, no explicit `cursor-pointer` but buttons are pointer by default in browsers. OK.
- **line 48-55** `<button>` English -- same. OK.

### Mobile adaptation
- Single column, full-width buttons with `py-5` padding. Good touch targets.
- No breakpoints needed.

### Loading / Empty states
- No API calls, N/A.

### Forms
- No forms.

#### Summary
- Pass -- no issues found.

---

## `pages/customer/MenuPage.tsx`

### Clickable elements
- **line 253-260** `<button>` expand header -- has `hover:text-foreground`. Missing explicit `cursor-pointer` (though browsers apply it to buttons).
- **line 344-355** Category sidebar `<button>` -- no `cursor-pointer`. Browser default applies but missing explicit class.
- **line 448-455** Add-to-cart `<Button>` -- shadcn, OK. `h-11 w-11` = 44px. Good.
- **line 504-508** Announcement dismiss `<button>` -- missing `cursor-pointer`.
- **line 277-298** Pay Now `<Button>` -- has `disabled={payingOrders}`. Good.

### Mobile adaptation
- `max-w-lg mx-auto` constrains width. Good.
- Category sidebar `w-24` with `text-sm`. Adequate.
- Search bar `h-10 text-base`. Good for mobile input.
- Add button `h-11 w-11` (44px). Good touch target.

### Loading / Empty states
- Loading state (lines 163-167). Plain text, no spinner.
- Error state (lines 168-172).
- Empty search results (line 364-368).
- Empty category (lines 378-382).

### Forms
- Search input has placeholder (line 249).

#### Summary
- **line 163-167** Loading state uses plain text, no spinner animation.
- **line 504-508** Dismiss button missing `cursor-pointer`.

---

## `pages/customer/CartPage.tsx`

### Clickable elements
- `<Button>` components throughout -- shadcn, OK.
- Quantity buttons `h-11 w-11` (44px). Good.
- Submit button has `disabled={submitting}`. Good.

### Mobile adaptation
- `max-w-lg mx-auto`. Good.
- `md:p-4` on card items. Good.
- Submit button `min-w-[120px] sm:min-w-[160px] min-h-[48px]`. Excellent.

### Loading / Empty states
- Empty cart state (lines 38-45). Good.
- No table state (lines 30-36). Good.
- Submit loading with Loader2 spinner (lines 201-204). Good.

### Forms
- Remark input has placeholder (line 167).
- Submit button has loading state (line 198-208). Good.

#### Summary
- Pass -- no issues found.

---

## `pages/customer/CheckoutPage.tsx`

### Clickable elements
- Payment submit `<Button>` disabled when `!stripe || processing`. Good.
- Tip buttons in `TipSelector` -- checked separately.

### Mobile adaptation
- `max-w-lg mx-auto`. Good.
- Submit button `min-h-[44px]`. Good.

### Loading / Empty states
- Stripe element loading spinner (lines 81-86). Good.
- No session fallback (lines 146-158). Good.

### Forms
- Stripe `PaymentElement` handles its own inputs. OK.
- Submit has loading state. Good.

#### Summary
- Pass -- no issues found.

---

## `pages/customer/OrderConfirmPage.tsx`

### Clickable elements
- All use `<Button>`. OK.
- "Call Staff" button (line 179) -- `size="sm"` with `h-7 px-3`. Height is 28px, below 44px minimum for mobile touch targets.

### Mobile adaptation
- `max-w-lg mx-auto`. Good.
- `md:pt-12` responsive padding. Good.

### Loading / Empty states
- Polling with spinner (lines 138-141). Good.
- Timeout fallback (lines 128-136). Good.
- Payment failed state (lines 52-65). Good.

### Forms
- No forms.

#### Summary
- **line 179** "Call Staff" button at `h-7` (28px) is below 44px minimum touch target.

---

## `pages/customer/OrderHistoryPage.tsx`

### Clickable elements
- Back button uses `<Button size="icon">` (36px default icon). OK for header.
- No other interactive elements.

### Mobile adaptation
- `max-w-lg mx-auto`. Good.
- Order cards full width. Good.

### Loading / Empty states
- Loading state (line 62). Plain text, no spinner.
- Empty state with icon + CTA button (lines 63-71). Good.

### Forms
- No forms.

#### Summary
- **line 62** Loading state uses plain text, no spinner.

---

## `pages/admin/LoginPage.tsx`

### Clickable elements
- Password toggle button (line 90-94) -- `p-1` padding. Total area approximately 24px. Below 44px touch target.
- Submit `<Button>` -- `min-h-[44px]`. Good.
- Submit has `disabled={loading}`. Good.

### Mobile adaptation
- `max-w-[440px]` centered. Good.
- All inputs have icons with `pl-9`. Good.

### Loading / Empty states
- Error display (lines 53-56). Good.
- Submit loading text (line 99). Good but no spinner.

### Forms
- All inputs have `placeholder`. Good.
- Submit has loading state. Good.
- `autoFocus` on username. Good.

#### Summary
- **line 90-94** Password toggle button area too small for touch (~24px).
- **line 99** Submit loading state uses text only, no spinner icon.

---

## `pages/admin/DashboardPage.tsx`

### Clickable elements
- Tab filter buttons use `<Button>`. OK.
- Action buttons have `min-h-[44px]`. Good.
- All have `disabled={updatingOrderId === order.id}`. Good.

### Mobile adaptation
- `max-w-4xl mx-auto`. Good.
- `px-3 md:px-4`, `py-3 md:py-4`. Good.
- `space-y-3 md:space-y-4`. Good.

### Loading / Empty states
- Loading state (lines 171-174). Plain text, no spinner.
- Empty state (lines 175-178). Plain text.

### Forms
- No forms.

#### Summary
- **line 171-174** Loading state uses plain text, no spinner.

---

## `pages/admin/MenuManagePage.tsx`

### Clickable elements
- `<Button>` components throughout. OK.
- `CategorySlider` scroll arrows (lines 188-189, 203-206) -- `p-1` padding. ~24px area. Below touch target.
- `SliderChip` buttons (lines 217-230) -- `px-3 py-1.5`. Height ~30px. Below 44px.
- Order sidebar `-/+` buttons (lines 283-291) -- `w-6 h-6` (24px). Severely below 44px minimum.
- Remove button (line 296-297) -- text link `text-xs`. Very small touch target.
- "Clear order" button (line 312) -- text link `text-xs`. Very small touch target.

### Mobile adaptation
- Menu manage uses `hidden lg:flex` for sidebar. Good.
- Category slider `overflow-x-auto scrollbar-hide`. Good.

### Loading / Empty states
- Loading spinner (line 91). Good.
- Empty state handled by `MenuItemTable`. OK.

### Forms
- No form inputs in main page (handled in `MenuItemForm` dialog).

#### Summary
- **lines 283-291** Order sidebar +/- buttons at 24px are below 44px minimum.
- **line 296-297** Remove button is a tiny text link, below 44px.
- **line 312** "Clear order" is a tiny text link, below 44px.
- **lines 188-189, 203-206** Scroll arrows at ~24px, below 44px.
- **lines 217-230** SliderChip buttons ~30px tall, below 44px.

---

## `pages/admin/CategoryManagePage.tsx`

### Clickable elements
- All `<Button>` with `min-h-[44px]` on mobile cards and desktop table. Good.
- Sort arrows (lines 303-304, 314-315) -- `min-h-[44px] min-w-[44px]`. Good.
- Inline edit spans use `cursor-pointer`. Good.
- `GripVertical` drag handle uses `cursor-grab active:cursor-grabbing`. Good.

### Mobile adaptation
- `md:hidden` card view for mobile. Good.
- `hidden md:block` table view for desktop. Good.
- Dialog `max-w-sm w-[calc(100vw-2rem)]`. Good.

### Loading / Empty states
- Loading spinner (lines 205-210). Good.
- Empty state in both mobile cards (line 261-263) and desktop table (lines 279-284). Good.

### Forms
- Dialog inputs have `placeholder`. Good.
- Save button has `disabled={saving || !editingCat.name.trim()}`. Good.
- Save button shows "Saving..." text. Good.

#### Summary
- Pass -- no issues found.

---

## `pages/admin/TablesPage.tsx`

### Clickable elements
- `ActionBtn` (lines 415-425) -- `py-3 px-4`. Adequate touch target.
- Mobile action buttons `size="sm"` with `flex-1 min-w-[120px]`. OK but height is `h-8` (32px), below 44px.
- Pencil icon on table list (line 190-191) -- `size-3` icon inside parent button. Very small ~12px target, though it piggybacks on the parent row.
- "New QR" button (line 345-346) -- `size="sm"` = 32px. Below 44px.
- Disable button (line 349-361) -- `size="sm"` = 32px. Below 44px.
- `showDisabled` checkbox (lines 170-174) -- native checkbox, small default size.

### Mobile adaptation
- `flex-col md:flex-row` layout. Good.
- Mobile dropdown selector (lines 118-145). Good.
- Mobile action area `lg:hidden` (lines 280-315). Good.
- Right sidebar `hidden lg:flex`. Good.

### Loading / Empty states
- Loading spinner (line 114). Good.
- Empty state when no table selected (lines 200-205). Good.
- Empty orders state (lines 229-231). Good.

### Forms
- Base URL input has placeholder (line 152). Good.
- Search menu input has placeholder (line 218). Good.

#### Summary
- **lines 288-313** Mobile action buttons at `size="sm"` (32px) below 44px minimum.
- **line 345-346** "New QR" button at `size="sm"` (32px) below 44px.
- **line 349-361** Disable button at `size="sm"` (32px) below 44px.
- **lines 170-174** Native checkbox is small for touch.

---

## `pages/admin/StoreSettingsPage.tsx`

### Clickable elements
- Save button `min-h-[44px]`. Good.
- `disabled={saving || !name.trim()}`. Good.
- "View Live Store" link uses `<Button asChild>` wrapping `<a>`. Good.

### Mobile adaptation
- `max-w-2xl mx-auto px-4 md:px-6 py-4 md:py-6`. Good.
- Save button `w-full md:w-auto`. Good.

### Loading / Empty states
- Loading state (lines 79-85). Plain text, no spinner.
- Store not found error (lines 87-93). Good.
- Success/error message (lines 196-200). Good.

### Forms
- All inputs have `placeholder`. Good.
- All inputs use `text-base` (avoids iOS zoom). Good.
- Save has loading state. Good.

#### Summary
- **lines 79-85** Loading state uses plain text, no spinner.

---

## `pages/admin/FloorPlanPage.tsx`

### Clickable elements
- Mobile panel toggle buttons -- `flex-1 px-3 py-2.5`. OK height ~36px. Slightly below 44px.
- Tab buttons (orders/waitlist) -- same sizing. ~36px.
- Zone filter `<Button size="sm">` -- 32px. Below 44px.

### Mobile adaptation
- `flex-col md:flex-row`. Good.
- Mobile panel toggles. Good.
- Right sidebar `hidden md:flex`. Good.
- `mobilePanel` state for toggling between map and orders. Good.

### Loading / Empty states
- Loading plain text (line 76). No spinner.

### Forms
- No forms.

#### Summary
- **line 76** Loading state uses plain text, no spinner.
- **various** Tab/zone buttons at ~32-36px, below 44px minimum.

---

## `pages/admin/FloorPlanEditorPage.tsx`

### Clickable elements
- `PropertiesPanel` shape buttons (lines 246-255) -- `py-1.5 text-xs`. ~24px height. Below 44px.
- Zone delete button (line 196-201) -- `w-4 h-4` (16px). Very small.
- "Add Zone" button (lines 205-207) -- `size="sm"` with `variant="ghost"`. 32px. Below 44px.
- `MobileBottomBar` shape buttons (lines 295-308) -- `px-2 py-1.5`. ~24px. Below 44px.
- Delete button in mobile bar (line 312) -- `size="sm"`. 32px. Below 44px.

### Mobile adaptation
- Desktop properties panel `hidden md:block`. Good.
- Mobile bottom bar for selected table (lines 272-318). Good.
- Zone selector in mobile bar. Good.

### Loading / Empty states
- Loading plain text (line 112). No spinner.
- Access restricted message (lines 106-110). OK.

### Forms
- Properties panel inputs have labels. Good.
- Name input has value binding. Good (no placeholder though).

#### Summary
- **line 112** Loading plain text, no spinner.
- **lines 246-255** Shape buttons at ~24px, below 44px.
- **line 196-201** Zone delete button at 16px, far below 44px.
- **lines 295-308** Mobile shape buttons at ~24px, below 44px.
- **line 227** Name input in PropertiesPanel has no `placeholder`.

---

## `pages/admin/AnalyticsPage.tsx`

### Clickable elements
- Date inputs (lines 88-98) -- `px-2 py-1 text-sm`. Small touch targets (~28px).
- Chart mode toggle buttons (lines 120-125) -- `px-3 py-1`. ~24px. Below 44px.
- Export CSV button uses `<Button>`. OK.

### Mobile adaptation
- `max-w-4xl mx-auto`. Good.
- `grid-cols-1 sm:grid-cols-3`. Good.
- Tables use `overflow-x-auto`. Good.

### Loading / Empty states
- Loading text (line 101). No spinner.
- Error display (line 102). Good.
- Empty top items (line 137). Good.
- Empty daily stats (line 170). Good.
- Staff section loads independently (line 279). Good.

### Forms
- Date inputs have `value` binding. Good. No `placeholder` but `type="date"` has native UI.

#### Summary
- **line 101** Loading state, no spinner.
- **lines 88-98** Date inputs at ~28px, below 44px.
- **lines 120-125** Chart toggle buttons at ~24px, below 44px.

---

## `pages/admin/CouponManagePage.tsx`

### Clickable elements
- Desktop table `<Switch>` and delete `<Button>` -- `size="sm"` (32px). Below 44px.
- Mobile card `<Switch>` and delete button -- same 32px. Below 44px.
- Form dialog buttons use `<Button>`. OK.

### Mobile adaptation
- `hidden md:block` for desktop table. Good.
- `md:hidden` for mobile cards. Good.
- Dialog `max-w-md w-[calc(100vw-2rem)]`. Good.

### Loading / Empty states
- Loading state (lines 103-105). Plain text, no spinner.
- Empty state (line 122). Good.
- Error display (line 120). Good.

### Forms
- Code input has `placeholder`. Good.
- All form inputs have placeholders. Good.
- Save button `disabled={saving || !form.code.trim()}`. Good.
- Saving text shown. Good.

#### Summary
- **lines 103-105** Loading state, no spinner.
- Desktop table action buttons at 32px, below 44px.

---

## `pages/admin/StaffManagePage.tsx`

### Clickable elements
- Tab buttons (lines 131-138) -- `px-4 py-2`. ~32px height. Below 44px.
- Delete button `size="sm"` (32px). Below 44px.
- Role edit/delete buttons `size="sm"` (32px). Below 44px.
- Permission checkboxes (lines 313-320) -- `px-2 py-1.5` label. OK area.

### Mobile adaptation
- `max-w-5xl mx-auto`. Good.
- Staff items use flex layout that works on mobile. Good.
- Dialog `max-w-md w-[calc(100vw-2rem)]`. Good.
- Role dialog `max-w-lg w-[calc(100vw-2rem)]`. Good.

### Loading / Empty states
- Loading state (line 123). Plain text, no spinner.
- Empty staff state (line 190). Good.
- Empty roles state (line 234). Good.
- Error display (line 127). Good.

### Forms
- All dialog inputs have `placeholder`. Good.
- Create button `disabled={saving || !form.username.trim() || !form.password.trim()}`. Good.
- Saving text shown. Good.

#### Summary
- **line 123** Loading state, no spinner.
- **lines 131-138** Tab buttons at ~32px, below 44px.
- Various action buttons at `size="sm"` (32px), below 44px.

---

## `components/layout/AdminLayout.tsx`

### Clickable elements
- Mobile hamburger button (lines 56-60) -- `min-h-[44px] min-w-[44px]`. Excellent.
- Language toggle (lines 64-67) -- `px-2.5 py-1`. ~24px height. Below 44px.
- Sidebar collapse button (line 85) -- `min-h-[44px] min-w-[44px]`. Good.
- NavLink items (lines 91-109) -- `px-4 py-2.5`. ~36px height. Slightly below 44px.
- Language button in sidebar (lines 116-125) -- `min-h-[44px]`. Good.
- Logout button (lines 131-137, 148-150) -- `min-h-[44px]`. Good.
- Mobile nav links (lines 167-184) -- `px-4 py-3`. ~42px. Close to 44px. Acceptable.
- Mobile language button (lines 187-192) -- `min-h-[44px]`. Good.

### Mobile adaptation
- `flex-col md:flex-row`. Good.
- Mobile top bar with sheet navigation. Good.
- Sidebar `hidden md:flex`. Good.

### Loading / Empty states
- N/A (layout component).

### Forms
- No forms.

#### Summary
- **lines 64-67** Mobile language toggle at ~24px, below 44px.
- **lines 91-109** Desktop NavLink items at ~36px, slightly below 44px.

---

## `components/layout/ProtectedRoute.tsx`

- Auth guard, no UI elements. No audit items.

---

## `components/shared/ImageUpload.tsx`

### Clickable elements
- "Change" button uses `<Button size="sm">`. 32px. Below 44px but OK for desktop-oriented form.
- "Remove" button uses `<Button size="sm">`. Same.
- Upload area (lines 81-88) -- `cursor-pointer`. Good. `min-h-[120px]`. Good.

### Mobile adaptation
- `max-w-[200px]`. Adequate.

### Loading / Empty states
- Upload overlay with "Uploading..." text (lines 53-57). Good.
- Error display (line 97). Good.

### Forms
- Hidden file input triggered by visible area. OK.

#### Summary
- Pass -- acceptable for form context.

---

## `components/shared/TipSelector.tsx`

### Clickable elements
- Tip preset buttons (lines 21-33) -- `py-2.5`. ~38px height. Slightly below 44px.
- "No tip" / custom button (lines 34-42) -- same sizing.

### Mobile adaptation
- `grid-cols-4` layout. Good.
- Full width card. Good.

### Loading / Empty states
- Loading spinner for tip recalculation (line 18). Good.

### Forms
- No form inputs.

#### Summary
- Tip buttons at ~38px, slightly below 44px recommended. Borderline acceptable.

---

## `components/menu/MenuItemDetailSheet.tsx`

### Clickable elements
- Quick tag buttons (lines 149-161) -- `px-3 py-2 min-h-[36px]`. 36px. Below 44px.
- Option choice buttons (lines 179-199) -- `px-4 py-3 min-h-[44px]`. Good.
- Quantity buttons `h-11 w-11` (44px). Good.
- Add to cart `min-h-[44px]`. Good.
- `disabled={!allRequiredSelected()}`. Good.

### Mobile adaptation
- Bottom sheet `max-h-[85vh]`. Good.
- `pb-safe` for bottom safe area. Good.

### Loading / Empty states
- N/A (data passed via props).

### Forms
- Remark textarea has `placeholder`. Good.

#### Summary
- **lines 149-161** Quick tag buttons at 36px, below 44px.

---

## `components/menu/MenuItemTable.tsx`

### Clickable elements
- `InlineEdit` spans use `cursor-pointer`. Good.
- Desktop action buttons `size="sm"` (32px). Below 44px for touch but desktop-only.
- Mobile card buttons `min-h-[44px]`. Good.
- Mobile switch component. OK.

### Mobile adaptation
- `hidden md:block` for desktop table. Good.
- `md:hidden` for mobile cards. Good.
- Mobile card items with proper spacing. Good.

### Loading / Empty states
- Empty category rows in both views. Good.

### Forms
- `InlineEdit` input is temporary and not a persistent form.

#### Summary
- Pass -- desktop-only action buttons at 32px acceptable for desktop context.

---

## `components/menu/MenuItemForm.tsx`

### Clickable elements
- Discount bar buttons (lines 358-364) -- `px-3 py-1 text-xs`. ~24px. Below 44px.
- "Free" button (line 363) -- same small size.
- "Reset" button (line 370) -- text link. Very small.
- Option/choice remove buttons `size="sm"` (32px). Below 44px.
- "Add Choice" button `size="sm"` (32px). Below 44px.
- Dialog cancel/save buttons -- default size. OK.

### Mobile adaptation
- Dialog `max-w-lg w-[calc(100vw-2rem)] md:w-auto max-h-[85vh]`. Good.
- `grid-cols-2 gap-3` for price/category. Good.

### Loading / Empty states
- Save button `disabled={saving || !editingItem.name}`. Good.
- Saving text shown. Good.

### Forms
- All inputs have `placeholder`. Good.
- All inputs use `text-base`. Good (prevents iOS zoom).

#### Summary
- **lines 358-364** Discount buttons at ~24px, below 44px.
- **line 370** Reset text link, very small touch target.
- **line 309** Option remove button at 32px, below 44px.

---

## `components/menu/ItemCustomizeView.tsx`

### Clickable elements
- Back button `h-8 w-8` (32px). Below 44px.
- Choice buttons `px-3 py-2 min-h-[36px]`. 36px. Below 44px.
- Quantity buttons `h-8 w-8` (32px). Below 44px.
- Confirm button `px-5` with `disabled={!allRequiredSelected}`. Good.

### Mobile adaptation
- Flex column full height. Good.
- Used inside `SheetContent` which handles width.

### Loading / Empty states
- N/A.

### Forms
- No form inputs.

#### Summary
- **line 46** Back button at 32px, below 44px.
- **line 76-88** Choice buttons at 36px, below 44px.
- **lines 99, 104** Quantity buttons at 32px, below 44px.

---

## `components/menu/CsvImportDialog.tsx`

### Clickable elements
- Close button `<button>` (line 151-153) -- no explicit sizing. Small text "x".
- Step navigation buttons use `<Button>`. OK.
- File upload area uses `<Button>`. OK.

### Mobile adaptation
- Dialog `max-w-2xl w-[calc(100vw-2rem)] max-h-[80vh]`. Good.
- Preview table `min-w-[400px]` with `overflow-x-auto`. Good.

### Loading / Empty states
- Import button shows "Importing..." text (line 255). Good.

### Forms
- Column mapping uses `<select>` elements with "-- Skip --" option. Good.
- File input hidden, triggered by button. Good.
- Import disabled when importing. Good.

#### Summary
- **line 151-153** Close button is a small "x" text, below 44px.

---

## `components/order/OrderCard.tsx`

### Clickable elements
- Card root -- `cursor-pointer`. Good.
- Reprint button `min-h-[44px]`. Good. `disabled={reprinting}`. Good.
- Edit button `min-h-[44px]`. Good.
- Action button from parent has `min-h-[44px]`. Good.

### Mobile adaptation
- `p-3 md:p-4` padding. Good.

### Loading / Empty states
- Reprint feedback shown (lines 102-103). Good.
- Reprint disabled during operation. Good.

### Forms
- No forms.

#### Summary
- Pass -- no issues found.

---

## `components/order/OrderDetailDialog.tsx`

### Clickable elements
- Print receipt button `min-h-[44px]`. Good.
- Edit button `min-h-[44px]`. Good.
- Status update buttons `min-h-[44px]` with `disabled={updating}`. Good.

### Mobile adaptation
- Dialog `max-w-md w-[calc(100vw-2rem)] max-h-[85vh]`. Good.

### Loading / Empty states
- Updating state shows "..." text. Good.

### Forms
- No forms.

#### Summary
- Pass -- no issues found.

---

## `components/order/OrderEditDialog.tsx`

### Clickable elements
- Quantity buttons `h-11 w-11` (44px). Good.
- Delete button `min-h-[44px]`. Good.
- Option choice buttons `min-h-[44px]`. Good.
- Save button `disabled={savingEdit || !canSave}`. Good.

### Mobile adaptation
- Dialog `max-w-lg w-[calc(100vw-2rem)] md:w-auto max-h-[85vh]`. Good.

### Loading / Empty states
- Saving state shown. Good.
- Missing required options warning (lines 236-245). Good.

### Forms
- Remark input has `placeholder`. Good.
- `text-base` class on remark. Good.
- Add dish selector with placeholder. Good.

#### Summary
- Pass -- no issues found.

---

## `components/order/OrderEditMode.tsx`

### Clickable elements
- Quantity buttons `min-h-[44px] min-w-[44px]`. Good.
- Delete button `min-h-[44px] min-w-[44px]`. Good.
- Discount buttons `size="xs"` (24px height). Below 44px.
- Add item panel items (lines 173-179) -- `px-2 py-1`. ~24px. Below 44px.
- Close "x" button `size="xs"` (24px). Below 44px.
- Save/cancel buttons `flex-1`. Default height. OK.

### Mobile adaptation
- Used inside a side panel, adequate for context.

### Loading / Empty states
- Save loading state `disabled={saving}`. Good.
- Empty items message (line 81). Good.
- No results in add panel (line 180). Good.

### Forms
- Remark input has `placeholder="Note..."`. Good.
- Search input has placeholder. Good.
- Save `disabled={saving || !items.length}`. Good.

#### Summary
- **lines 96-99** Discount buttons at `size="xs"` (24px), below 44px.
- **lines 173-179** Add item options at ~24px, below 44px.

---

## `components/order/OrderReceipt.tsx`

- Print-only component, rendered hidden. No interactive elements.

#### Summary
- Pass -- N/A.

---

## `components/order/OrderingSheet.tsx`

### Clickable elements
- Category grid buttons (lines 129-135) -- `p-4`. Good touch target.
- Back button `h-8 w-8` (32px). Below 44px.
- Item `+` button `h-8 w-8` (32px). Below 44px.
- Quantity buttons `h-8 w-8` (32px). Below 44px.
- Send button full width `disabled={sending}`. Good.

### Mobile adaptation
- Sheet `w-full max-w-md`. Good.
- Full height flex column. Good.

### Loading / Empty states
- Loading spinner (lines 122-125). Good.
- Sending state with spinner (line 179). Good.

### Forms
- No form inputs.

#### Summary
- **line 110** Back button at 32px, below 44px.
- **lines 152-156** Quantity +/- buttons at 32px, below 44px.
- **line 161** Add button at 32px, below 44px.

---

## `components/table/TableGrid.tsx`

### Clickable elements
- Card `cursor-pointer`. Good.
- `onClick` handler on cards. Good.
- Cards lack `role="button"`, `tabIndex`, and `onKeyDown` for keyboard accessibility.

### Mobile adaptation
- `grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5`. Good.
- `min-h-[100px]` on cards. Good.

### Loading / Empty states
- Empty state (lines 32-37). Good.

### Forms
- No forms.

#### Summary
- Cards lack `role="button"`, `tabIndex`, `onKeyDown` for keyboard accessibility.

---

## `components/table/TransferTableDialog.tsx`

### Clickable elements
- Table selection cards -- `cursor-pointer`. Good.
- Cards lack `role="button"`, `tabIndex`, `onKeyDown` for keyboard accessibility.
- Transfer button `disabled={!selectedId || transferring}`. Good.

### Mobile adaptation
- Dialog `max-w-md w-[calc(100vw-2rem)] md:w-auto max-h-[85vh]`. Good.
- `grid-cols-2` for table cards. Good.

### Loading / Empty states
- Loading text (lines 76-78). Good.
- No available tables state (lines 79-82). Good.

### Forms
- No form inputs.

#### Summary
- Table selection cards missing keyboard accessibility (`role`, `tabIndex`, `onKeyDown`).

---

## `components/table/CloseTableDialog.tsx`

### Clickable elements
- Cancel button uses `<Button>`. OK.
- Close button `variant="destructive"` with `disabled={closing}`. Good.

### Mobile adaptation
- Dialog `max-w-md w-[calc(100vw-2rem)] md:w-auto max-h-[85vh]`. Good.

### Loading / Empty states
- Loading text (line 73). Good.
- Empty orders state (lines 74-77). Good.
- Error display (line 112). Good.

### Forms
- No forms.

#### Summary
- Pass -- no issues found.

---

## `components/table/TableCrudDialog.tsx`

### Clickable elements
- Cancel/Save buttons use `<Button>`. OK.
- Save `disabled={saving || (isNew && allFull)}`. Good.

### Mobile adaptation
- Dialog `max-w-sm w-[calc(100vw-2rem)]`. Good.

### Loading / Empty states
- Error display (line 83). Good.
- "All tables full" warning (lines 70-72). Good.

### Forms
- Name input has `placeholder`. Good.
- Table number input present. Good.
- Number hint text (line 69). Good.
- `autoFocus` on name. Good.

#### Summary
- Pass -- no issues found.

---

## `components/table/TableDetailPanel.tsx`

### Clickable elements
- Tab buttons (lines 89-94) -- `px-3 py-2`. ~32px. Below 44px.
- Action buttons `min-h-[36px]`. Below 44px.
- Edit/Split/Transfer buttons `size="sm"` with `min-h-[36px]`. Below 44px.

### Mobile adaptation
- Sheet `w-full sm:w-[380px]`. Good.
- ScrollArea for overflow. Good.

### Loading / Empty states
- Loading text (line 99). Good.
- No active order state (line 127). Good.
- No past orders state (line 209). Good.

### Forms
- No forms.

#### Summary
- **lines 89-94** Tab buttons at ~32px, below 44px.
- **lines 142-149** Action buttons at `min-h-[36px]`, below 44px.

---

## `components/table/BillSettleDialog.tsx`

### Clickable elements
- "Pay in Full" button uses `<Button>` with `disabled={loading}`. Good.
- "Mark Paid" button `size="sm"`. 32px. Below 44px.
- "Remove coupon" button -- text link. Very small.
- Apply coupon button `size="sm"`. 32px. Below 44px.
- "Create Splits" button uses `<Button>`. OK.
- "Back" button uses `<Button>`. OK.
- Split items `py-3`. Good row height.

### Mobile adaptation
- Dialog `max-w-md w-[calc(100vw-2rem)] max-h-[85vh]`. Good.

### Loading / Empty states
- Pay in full loading spinner (line 206). Good.
- Settled state message (lines 128-132). Good.

### Forms
- Coupon code input has `placeholder`. Good.
- Split count input has `min` and `max`. Good.
- Apply disabled when no code. Good.

#### Summary
- **line 177** "Remove coupon" text link, very small touch target.
- **line 251** "Mark Paid" button at 32px, below 44px.

---

## `components/table/SplitBillDialog.tsx`

### Clickable elements
- Mode toggle buttons use `<Button>`. OK.
- Generate button `disabled={loading || !allAssigned}`. Good.
- People input. OK.
- Item assignment `<select>` elements -- `px-2 py-1 text-xs`. Small.

### Mobile adaptation
- Dialog `max-w-md sm:max-w-lg w-[calc(100vw-2rem)] md:w-auto max-h-[85vh]`. Good.

### Loading / Empty states
- Loading text on generate button. Good.
- Error display (line 100). Good.

### Forms
- People input has bound value. No explicit placeholder but `type="number"` with `min`. OK.
- Assignment selects have disabled placeholder option. Good.

#### Summary
- **line 150-153** Item assignment selects at `text-xs` are small for touch.

---

## `components/floor/ActiveOrdersSidebar.tsx`

### Clickable elements
- Order cards `role="button" tabIndex={0}`. Good.
- `onKeyDown` handler for Enter. Good.
- `cursor-pointer`. Good.

### Mobile adaptation
- `w-80` fixed width (used inside FloorPlanPage which handles layout). OK.

### Loading / Empty states
- Empty state "No active orders" (lines 118-121). Good.
- Auto-refresh every 15s. Good.

### Forms
- No forms.

#### Summary
- Pass -- no issues found. (Notably good keyboard accessibility on order cards.)

---

## `components/floor/WaitlistPanel.tsx`

### Clickable elements
- "Seat" button `size="sm"` (32px). Below 44px.
- "Remove" button `size="sm" variant="destructive"` (32px). Below 44px.
- Submit button full width `disabled={loading}`. Good.

### Mobile adaptation
- Inputs full-width in `flex gap-2`. Good.
- List items `p-3` with flex layout. Good.

### Loading / Empty states
- Loading state on submit button. Good.
- Empty state (lines 114-117). Good.
- Error display (line 109). Good.

### Forms
- Name input has `placeholder`. Good.
- Party size has `placeholder` and `min`. Good.
- Phone has `placeholder`. Good.
- Submit `disabled={loading}`. Good.

#### Summary
- **line 164** "Seat" button at 32px, below 44px.
- **line 168** "Remove" button at 32px, below 44px.

---

## `components/floor/FloorTableShape.tsx`

### Clickable elements
- SVG `<g>` with `cursor: pointer` when onClick is provided. Good.
- No keyboard accessibility on SVG elements (standard limitation).

### Mobile adaptation
- SVG scales via `viewBox`. Good.

### Loading / Empty states
- N/A (pure presentational).

### Forms
- No forms.

#### Summary
- SVG table shapes lack keyboard accessibility (difficult to fix in SVG context). Known limitation.

---

## `components/floor/FloorCanvas.tsx`

### Clickable elements
- Table shapes delegate to `FloorTableShape` with onClick. Good.
- Touch handlers for drag in editor mode (lines 67-77). Good.
- No keyboard drag support.

### Mobile adaptation
- `style={{ minHeight: 300 }}`. Good.
- `w-full`. Good.
- Touch handlers for mobile drag. Good.

### Loading / Empty states
- N/A.

### Forms
- No forms.

#### Summary
- No keyboard drag support (acceptable for canvas editor).

---

## Aggregate Summary

### Most Critical Issues (High Impact)

| # | File | Issue |
|---|------|-------|
| 1 | `MenuManagePage.tsx` (OrderSidebar) | +/- buttons at 24px, far below 44px touch minimum |
| 2 | `OrderEditMode.tsx` | Discount buttons at 24px (`size="xs"`) |
| 3 | `FloorPlanEditorPage.tsx` | Zone delete button at 16px; shape buttons at 24px |
| 4 | `ItemCustomizeView.tsx` | Back + quantity buttons at 32px |
| 5 | `OrderingSheet.tsx` | All item interaction buttons at 32px |
| 6 | `TablesPage.tsx` | Mobile action buttons at 32px |

### Recurring Patterns

**Loading states without spinner** (plain text only):
- `MenuPage.tsx` (line 163)
- `OrderHistoryPage.tsx` (line 62)
- `DashboardPage.tsx` (line 171)
- `StoreSettingsPage.tsx` (line 79)
- `FloorPlanPage.tsx` (line 76)
- `FloorPlanEditorPage.tsx` (line 112)
- `AnalyticsPage.tsx` (line 101)
- `CouponManagePage.tsx` (line 103)
- `StaffManagePage.tsx` (line 123)

**Buttons below 44px minimum touch target** (admin pages):
Widespread across admin components. Most `size="sm"` buttons are 32px (h-8).
This is borderline for desktop admin interfaces but problematic for
tablets and touch-screen POS systems.

**Missing keyboard accessibility on clickable cards**:
- `TableGrid.tsx` -- cards lack `role="button"` / `tabIndex` / `onKeyDown`
- `TransferTableDialog.tsx` -- table cards same issue
- (Counter-example: `ActiveOrdersSidebar.tsx` does it correctly)

**Missing `cursor-pointer` on native `<button>` elements**:
- `ScanPage.tsx` -- Continue and Skip buttons
- `MenuPage.tsx` -- Dismiss announcement button
- Note: browsers apply `cursor: pointer` to `<button>` by default, so this
  is a code consistency issue rather than a functional bug.

### Files with No Issues

The following files had no UX issues found:
- `App.tsx`, `main.tsx`
- `LangSelectPage.tsx`
- `CartPage.tsx`
- `CheckoutPage.tsx`
- `ProtectedRoute.tsx`
- `CategoryManagePage.tsx`
- `OrderCard.tsx`
- `OrderDetailDialog.tsx`
- `OrderEditDialog.tsx`
- `OrderReceipt.tsx`
- `CloseTableDialog.tsx`
- `TableCrudDialog.tsx`
- `ActiveOrdersSidebar.tsx`
- `ImageUpload.tsx`
- `FloorTableShape.tsx`
- `FloorCanvas.tsx`
- `MenuItemTable.tsx`
- All `components/ui/*.tsx` primitives

---

### Priority Recommendations

**P0 -- Fix immediately (customer-facing or frequent admin touch)**
1. Add spinner to customer-facing loading states (`MenuPage`, `OrderHistoryPage`)
2. Fix `OrderConfirmPage` "Call Staff" button touch target (28px -> 44px)

**P1 -- Fix soon (admin UX on tablets/touch)**
1. `MenuManagePage` OrderSidebar +/- buttons: increase from 24px to at least 44px
2. `OrderEditMode` discount buttons: increase from `size="xs"` to at least `size="sm"`
3. `ItemCustomizeView` + `OrderingSheet`: increase all 32px buttons to 44px
4. `TablesPage` mobile action buttons: add `min-h-[44px]`

**P2 -- Backlog (polish)**
1. Add spinners to all admin loading states (9 files listed above)
2. Add keyboard accessibility to `TableGrid` and `TransferTableDialog` cards
3. Increase `FloorPlanEditorPage` shape/zone buttons to 44px
4. Increase `WaitlistPanel` Seat/Remove buttons to 44px
5. Make `LoginPage` password toggle button larger
