# i18n Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add full Chinese/English internationalization to the QR ordering system — UI text + bilingual menu item data.

**Architecture:** react-i18next with namespace separation (common/customer/admin). Bilingual menu data via optional `nameEn`/`descriptionEn` fields on existing types. Language persisted in localStorage with URL param override. Collapsible admin sidebar with language toggle.

**Tech Stack:** react-i18next, i18next, i18next-browser-languagedetector

---

## File Structure

```
# New files
client/src/i18n/index.ts                    # i18next init config
client/src/i18n/zh/common.json              # Shared Chinese translations
client/src/i18n/zh/customer.json            # Customer-side Chinese
client/src/i18n/zh/admin.json               # Admin-side Chinese
client/src/i18n/en/common.json              # Shared English translations
client/src/i18n/en/customer.json            # Customer-side English
client/src/i18n/en/admin.json               # Admin-side English
client/src/lib/i18n-utils.ts                # localized() helper
client/src/pages/customer/LangSelectPage.tsx # Language selection page

# Modified files
shared/types.ts                              # Add nameEn/descriptionEn fields
server/src/seed.ts                           # Add English names to seed data
server/src/controllers/menu.controller.ts    # Pass through new fields
client/package.json                          # Add i18n dependencies
client/src/App.tsx                           # Add routes, wrap with I18nextProvider
client/src/components/AdminLayout.tsx         # Collapsible sidebar + lang toggle
client/src/pages/customer/ScanPage.tsx        # Check lang, redirect to lang-select if needed
client/src/pages/customer/MenuPage.tsx        # Use localized() + t()
client/src/pages/customer/CartPage.tsx        # Use t()
client/src/pages/customer/OrderConfirmPage.tsx # Use t()
client/src/pages/admin/LoginPage.tsx          # Use t()
client/src/pages/admin/DashboardPage.tsx      # Use t()
client/src/pages/admin/MenuManagePage.tsx      # Add nameEn fields + use t()
client/src/pages/admin/CategoryManagePage.tsx  # Add nameEn field + use t()
client/src/pages/admin/TablesPage.tsx          # Use t()
client/src/pages/admin/StoreSettingsPage.tsx   # Use t()
client/src/components/OrderDetailDialog.tsx    # Use t()
client/src/components/OrderReceipt.tsx         # Use t()
```

---

## Chunk 1: Foundation (types, deps, i18n config, helper)

### Task 1: Install dependencies

**Files:**
- Modify: `client/package.json`

- [ ] **Step 1: Install react-i18next and dependencies**

```bash
cd /Users/evergreen/Desktop/个人代码/QR_Code
pnpm --filter client add react-i18next i18next i18next-browser-languagedetector
```

- [ ] **Step 2: Verify installation**

```bash
pnpm --filter client exec -- node -e "require('react-i18next')"
```
Expected: no error

- [ ] **Step 3: Commit**

```bash
git add client/package.json pnpm-lock.yaml
git commit -m "chore: add react-i18next dependencies"
```

---

### Task 2: Update shared types

**Files:**
- Modify: `shared/types.ts`

- [ ] **Step 1: Add nameEn/descriptionEn to MenuItem**

In `shared/types.ts`, add to the `MenuItem` interface after `name`:
```typescript
nameEn?: string
```
After `description`:
```typescript
descriptionEn?: string
```

- [ ] **Step 2: Add nameEn to Category**

After the `name` field in `Category`:
```typescript
nameEn?: string
```

- [ ] **Step 3: Add nameEn to MenuItemOption**

After the `name` field in `MenuItemOption`:
```typescript
nameEn?: string
```

- [ ] **Step 4: Add nameEn to MenuItemOptionChoice**

After the `name` field in `MenuItemOptionChoice`:
```typescript
nameEn?: string
```

- [ ] **Step 5: Add nameEn to SelectedOption and OrderItem**

`SelectedOption` — add after `optionName`:
```typescript
optionNameEn?: string
```
After `choiceName`:
```typescript
choiceNameEn?: string
```

`OrderItem` — add after `name`:
```typescript
nameEn?: string
```

- [ ] **Step 6: Commit**

```bash
git add shared/types.ts
git commit -m "feat: add English name fields to shared types"
```

---

### Task 3: Create i18n config

**Files:**
- Create: `client/src/i18n/index.ts`

- [ ] **Step 1: Create i18n init file**

```typescript
import i18n from 'i18next'
import { initReactI18next } from 'react-i18next'
import LanguageDetector from 'i18next-browser-languagedetector'

import zhCommon from './zh/common.json'
import zhCustomer from './zh/customer.json'
import zhAdmin from './zh/admin.json'
import enCommon from './en/common.json'
import enCustomer from './en/customer.json'
import enAdmin from './en/admin.json'

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      zh: { common: zhCommon, customer: zhCustomer, admin: zhAdmin },
      en: { common: enCommon, customer: enCustomer, admin: enAdmin },
    },
    fallbackLng: 'zh',
    defaultNS: 'common',
    ns: ['common', 'customer', 'admin'],
    interpolation: { escapeValue: false },
    detection: {
      order: ['querystring', 'localStorage', 'navigator'],
      lookupQuerystring: 'lang',
      lookupLocalStorage: 'i18n-lang',
      caches: ['localStorage'],
    },
  })

export default i18n
```

- [ ] **Step 2: Import i18n in App entry**

In `client/src/main.tsx` (or wherever the app mounts), add before the render:
```typescript
import './i18n'
```

- [ ] **Step 3: Commit**

```bash
git add client/src/i18n/index.ts client/src/main.tsx
git commit -m "feat: configure react-i18next with language detection"
```

---

### Task 4: Create translation files

**Files:**
- Create: `client/src/i18n/zh/common.json`
- Create: `client/src/i18n/zh/customer.json`
- Create: `client/src/i18n/zh/admin.json`
- Create: `client/src/i18n/en/common.json`
- Create: `client/src/i18n/en/customer.json`
- Create: `client/src/i18n/en/admin.json`

- [ ] **Step 1: Create zh/common.json**

```json
{
  "loading": "加载中...",
  "save": "保存",
  "saving": "保存中...",
  "cancel": "取消",
  "edit": "编辑",
  "delete": "删除",
  "confirm": "确认",
  "add": "添加",
  "back": "返回",
  "total": "合计",
  "status": {
    "pending": "待处理",
    "preparing": "制作中",
    "completed": "已完成"
  },
  "soldOut": "已售罄",
  "items": "件",
  "langSwitch": "EN"
}
```

- [ ] **Step 2: Create en/common.json**

```json
{
  "loading": "Loading...",
  "save": "Save",
  "saving": "Saving...",
  "cancel": "Cancel",
  "edit": "Edit",
  "delete": "Delete",
  "confirm": "Confirm",
  "add": "Add",
  "back": "Back",
  "total": "Total",
  "status": {
    "pending": "Pending",
    "preparing": "Preparing",
    "completed": "Completed"
  },
  "soldOut": "Sold out",
  "items": "items",
  "langSwitch": "中"
}
```

- [ ] **Step 3: Create zh/customer.json**

```json
{
  "langSelect": {
    "title": "请选择语言",
    "subtitle": "Please select your language"
  },
  "menu": {
    "searchPlaceholder": "搜索菜品...",
    "noResults": "未找到相关菜品",
    "selectSpec": "选规格",
    "from": "起",
    "required": "必选",
    "addToCart": "加入购物车",
    "goOrder": "去下单",
    "viewOrders": "查看订单"
  },
  "cart": {
    "title": "我的订单",
    "tableLabel": "桌号",
    "noTable": "未找到桌台信息",
    "scanPrompt": "请扫描桌上的二维码开始点餐",
    "emptyCart": "购物车为空",
    "emptyPrompt": "去菜单选点好吃的吧",
    "backToMenu": "返回菜单",
    "previousOrders": "已点菜品（共 {{count}} 单）",
    "previousTotal": "已点合计",
    "addMore": "本次加菜",
    "currentCart": "当前购物车",
    "perServing": "/ 份",
    "remarkPlaceholder": "添加备注（可选）",
    "itemCount": "{{count}} 件",
    "submitOrder": "提交订单",
    "submitAddMore": "提交加菜",
    "submitting": "提交中...",
    "continueOrder": "继续点菜"
  },
  "orderConfirm": {
    "notFound": "未找到订单",
    "notFoundPrompt": "请返回菜单重新点餐",
    "success": "下单成功！",
    "successPrompt": "您的订单已提交，请耐心等待",
    "orderNumber": "订单号",
    "orderDetail": "订单明细",
    "continueOrder": "继续点菜",
    "viewAllOrders": "查看全部订单"
  },
  "scan": {
    "error": "无法连接到服务器，请检查网络连接"
  }
}
```

- [ ] **Step 4: Create en/customer.json**

```json
{
  "langSelect": {
    "title": "请选择语言",
    "subtitle": "Please select your language"
  },
  "menu": {
    "searchPlaceholder": "Search dishes...",
    "noResults": "No dishes found",
    "selectSpec": "Options",
    "from": "+",
    "required": "Required",
    "addToCart": "Add to cart",
    "goOrder": "Place order",
    "viewOrders": "View orders"
  },
  "cart": {
    "title": "My Orders",
    "tableLabel": "Table",
    "noTable": "No table found",
    "scanPrompt": "Please scan the QR code on your table to start ordering",
    "emptyCart": "Cart is empty",
    "emptyPrompt": "Go check out the menu!",
    "backToMenu": "Back to menu",
    "previousOrders": "Previous orders ({{count}} total)",
    "previousTotal": "Previous total",
    "addMore": "Add more",
    "currentCart": "Current cart",
    "perServing": "/ each",
    "remarkPlaceholder": "Add a note (optional)",
    "itemCount": "{{count}} items",
    "submitOrder": "Place order",
    "submitAddMore": "Add to order",
    "submitting": "Submitting...",
    "continueOrder": "Order more"
  },
  "orderConfirm": {
    "notFound": "Order not found",
    "notFoundPrompt": "Please go back to the menu and try again",
    "success": "Order placed!",
    "successPrompt": "Your order has been submitted. Please wait.",
    "orderNumber": "Order #",
    "orderDetail": "Order details",
    "continueOrder": "Order more",
    "viewAllOrders": "View all orders"
  },
  "scan": {
    "error": "Cannot connect to server. Please check your connection."
  }
}
```

- [ ] **Step 5: Create zh/admin.json**

```json
{
  "nav": {
    "title": "扫码点餐",
    "subtitle": "后台管理",
    "orders": "订单管理",
    "menu": "菜品管理",
    "categories": "分类管理",
    "tables": "桌台管理",
    "settings": "门店设置",
    "owner": "店主",
    "staff": "员工",
    "logout": "退出"
  },
  "dashboard": {
    "title": "订单管理",
    "all": "全部",
    "noOrders": "暂无订单",
    "startPreparing": "开始制作",
    "markComplete": "完成",
    "editOrder": "修改",
    "editOrderTitle": "修改订单 #{{number}}",
    "addDishPlaceholder": "添加菜品...",
    "addDish": "添加",
    "missingOptions": "以下菜品缺少必选规格：",
    "saveChanges": "保存修改",
    "remark": "备注..."
  },
  "menuManage": {
    "title": "菜品管理",
    "itemCount": "{{count}} 道菜品",
    "catCount": "{{count}} 个分类",
    "addItem": "+ 添加菜品",
    "previewMode": "顾客预览",
    "tableMode": "表格视图",
    "dishName": "菜名",
    "dishNameEn": "English Name",
    "price": "价格",
    "priceYuan": "价格（元）",
    "specs": "规格",
    "status": "状态",
    "actions": "操作",
    "noItems": "暂无菜品",
    "delisted": "已下架",
    "addItemTitle": "添加菜品",
    "editItemTitle": "编辑菜品",
    "description": "描述",
    "descriptionEn": "English Description",
    "category": "分类",
    "selectCategory": "选择分类",
    "sortOrder": "排序",
    "listed": "上架中",
    "options": "规格选项",
    "addOption": "+ 添加规格",
    "noOptions": "暂无规格，点击\"添加规格\"来设置辣度、口味等选项",
    "optionName": "规格名称（如：辣度）",
    "optionNameEn": "English name (e.g. Spice Level)",
    "required": "必选",
    "choiceName": "选项名（如：微辣）",
    "choiceNameEn": "English (e.g. Mild)",
    "addChoice": "+ 添加选项",
    "clickToEdit": "点击编辑"
  },
  "categoryManage": {
    "title": "分类管理",
    "catCount": "{{count}} 个分类",
    "addCategory": "+ 添加分类",
    "sortOrder": "排序",
    "categoryName": "分类名称",
    "categoryNameEn": "English Name",
    "itemCount": "菜品数",
    "actions": "操作",
    "emptyPrompt": "暂无分类，点击\"添加分类\"创建",
    "addTitle": "添加分类",
    "editTitle": "编辑分类",
    "nameLabel": "分类名称 *",
    "nameEnLabel": "English Name",
    "namePlaceholder": "例：热菜、凉菜、饮品",
    "nameEnPlaceholder": "e.g. Hot Dishes, Cold Dishes",
    "sortLabel": "排序号",
    "sortHint": "数字越小越靠前",
    "clickToRename": "点击输入排序号"
  },
  "tables": {
    "title": "桌台管理",
    "summary": "共 {{total}} 张桌台 · 空闲 {{idle}} · 使用中 {{occupied}}",
    "addTable": "+ 添加桌台",
    "printAll": "全部打印",
    "baseUrl": "Base URL:",
    "baseUrlWarning": "Base URL 使用了 localhost，手机扫码将无法访问。请改为局域网 IP 地址。",
    "idle": "空闲",
    "occupied": "使用中",
    "scanToOrder": "扫码点餐",
    "print": "打印",
    "settle": "结算",
    "addTitle": "添加桌台",
    "editTitle": "编辑桌台",
    "nameLabel": "桌台名称 *",
    "namePlaceholder": "例：A1、B2、包间1",
    "nameDuplicate": "名称不能与现有桌台重复",
    "deleteOccupied": "桌台\"{{name}}\"正在使用中，无法删除。请先结算。",
    "confirmDelete": "确定删除桌台\"{{name}}\"？",
    "confirmSettle": "确定结算桌台\"{{name}}\"？所有未完成订单将标记为已完成。",
    "settled": "已结算 {{count}} 个订单",
    "renameFailed": "重命名失败",
    "saveFailed": "保存失败"
  },
  "storeSettings": {
    "title": "门店设置",
    "basicInfo": "基本信息",
    "storeName": "店名 *",
    "storeNamePlaceholder": "输入店名",
    "description": "门店描述",
    "descriptionPlaceholder": "简短介绍你的餐厅",
    "hours": "营业时间",
    "hoursPlaceholder": "例：周一至周五 10:00-22:00",
    "announcement": "门店公告",
    "announcementPlaceholder": "顾客扫码后会看到此公告",
    "loadFailed": "加载门店信息失败",
    "nameRequired": "店名不能为空",
    "saveSuccess": "保存成功",
    "saveFailed": "保存失败",
    "storeNotFound": "门店不存在"
  },
  "login": {
    "title": "后台登录",
    "storeId": "门店 ID",
    "username": "用户名",
    "password": "密码",
    "submit": "登录",
    "submitting": "登录中..."
  },
  "orderDetail": {
    "receipt": "小票",
    "table": "桌台",
    "customer": "顾客",
    "orderTime": "下单时间",
    "updateTime": "更新时间",
    "itemDetail": "菜品明细",
    "remarkPrefix": "备注",
    "printReceipt": "打印小票",
    "editOrder": "修改订单",
    "startPreparing": "开始制作",
    "markComplete": "完成"
  },
  "receipt": {
    "title": "扫码点餐",
    "orderNumber": "订单号",
    "table": "桌台",
    "customer": "顾客",
    "time": "时间",
    "thanks": "谢谢惠顾"
  }
}
```

- [ ] **Step 6: Create en/admin.json**

```json
{
  "nav": {
    "title": "QR Order",
    "subtitle": "Admin Panel",
    "orders": "Orders",
    "menu": "Menu",
    "categories": "Categories",
    "tables": "Tables",
    "settings": "Settings",
    "owner": "Owner",
    "staff": "Staff",
    "logout": "Logout"
  },
  "dashboard": {
    "title": "Order Management",
    "all": "All",
    "noOrders": "No orders",
    "startPreparing": "Start",
    "markComplete": "Done",
    "editOrder": "Edit",
    "editOrderTitle": "Edit Order #{{number}}",
    "addDishPlaceholder": "Add dish...",
    "addDish": "Add",
    "missingOptions": "Missing required options:",
    "saveChanges": "Save changes",
    "remark": "Notes..."
  },
  "menuManage": {
    "title": "Menu Management",
    "itemCount": "{{count}} dishes",
    "catCount": "{{count}} categories",
    "addItem": "+ Add Dish",
    "previewMode": "Preview",
    "tableMode": "Table View",
    "dishName": "Name",
    "dishNameEn": "English Name",
    "price": "Price",
    "priceYuan": "Price (¥)",
    "specs": "Options",
    "status": "Status",
    "actions": "Actions",
    "noItems": "No dishes",
    "delisted": "Unavailable",
    "addItemTitle": "Add Dish",
    "editItemTitle": "Edit Dish",
    "description": "Description",
    "descriptionEn": "English Description",
    "category": "Category",
    "selectCategory": "Select category",
    "sortOrder": "Sort Order",
    "listed": "Available",
    "options": "Options",
    "addOption": "+ Add Option",
    "noOptions": "No options. Click \"Add Option\" to set up variants like spice level, size, etc.",
    "optionName": "Option name (e.g. Spice Level)",
    "optionNameEn": "English name",
    "required": "Required",
    "choiceName": "Choice (e.g. Mild)",
    "choiceNameEn": "English (e.g. Mild)",
    "addChoice": "+ Add Choice",
    "clickToEdit": "Click to edit"
  },
  "categoryManage": {
    "title": "Category Management",
    "catCount": "{{count}} categories",
    "addCategory": "+ Add Category",
    "sortOrder": "Sort",
    "categoryName": "Name",
    "categoryNameEn": "English Name",
    "itemCount": "Dishes",
    "actions": "Actions",
    "emptyPrompt": "No categories. Click \"Add Category\" to create one.",
    "addTitle": "Add Category",
    "editTitle": "Edit Category",
    "nameLabel": "Category Name *",
    "nameEnLabel": "English Name",
    "namePlaceholder": "e.g. Hot Dishes, Cold Dishes",
    "nameEnPlaceholder": "e.g. Hot Dishes",
    "sortLabel": "Sort Order",
    "sortHint": "Lower number = higher position",
    "clickToRename": "Click to set sort order"
  },
  "tables": {
    "title": "Table Management",
    "summary": "{{total}} tables · {{idle}} idle · {{occupied}} occupied",
    "addTable": "+ Add Table",
    "printAll": "Print All",
    "baseUrl": "Base URL:",
    "baseUrlWarning": "Base URL uses localhost. Mobile devices cannot access it. Use your LAN IP address instead.",
    "idle": "Idle",
    "occupied": "Occupied",
    "scanToOrder": "Scan to Order",
    "print": "Print",
    "settle": "Settle",
    "addTitle": "Add Table",
    "editTitle": "Edit Table",
    "nameLabel": "Table Name *",
    "namePlaceholder": "e.g. A1, B2, Room 1",
    "nameDuplicate": "Name already exists",
    "deleteOccupied": "Table \"{{name}}\" is occupied. Please settle first.",
    "confirmDelete": "Delete table \"{{name}}\"?",
    "confirmSettle": "Settle table \"{{name}}\"? All pending orders will be marked as completed.",
    "settled": "Settled {{count}} orders",
    "renameFailed": "Rename failed",
    "saveFailed": "Save failed"
  },
  "storeSettings": {
    "title": "Store Settings",
    "basicInfo": "Basic Info",
    "storeName": "Store Name *",
    "storeNamePlaceholder": "Enter store name",
    "description": "Description",
    "descriptionPlaceholder": "A short intro for your restaurant",
    "hours": "Opening Hours",
    "hoursPlaceholder": "e.g. Mon-Fri 10:00-22:00",
    "announcement": "Announcement",
    "announcementPlaceholder": "Customers will see this after scanning",
    "loadFailed": "Failed to load store info",
    "nameRequired": "Store name is required",
    "saveSuccess": "Saved successfully",
    "saveFailed": "Save failed",
    "storeNotFound": "Store not found"
  },
  "login": {
    "title": "Admin Login",
    "storeId": "Store ID",
    "username": "Username",
    "password": "Password",
    "submit": "Login",
    "submitting": "Logging in..."
  },
  "orderDetail": {
    "receipt": "Receipt",
    "table": "Table",
    "customer": "Customer",
    "orderTime": "Order Time",
    "updateTime": "Updated",
    "itemDetail": "Items",
    "remarkPrefix": "Note",
    "printReceipt": "Print Receipt",
    "editOrder": "Edit Order",
    "startPreparing": "Start",
    "markComplete": "Done"
  },
  "receipt": {
    "title": "QR Order",
    "orderNumber": "Order #",
    "table": "Table",
    "customer": "Customer",
    "time": "Time",
    "thanks": "Thank you!"
  }
}
```

- [ ] **Step 7: Commit**

```bash
git add client/src/i18n/
git commit -m "feat: add zh/en translation files for all namespaces"
```

---

### Task 5: Create localized() helper

**Files:**
- Create: `client/src/lib/i18n-utils.ts`

- [ ] **Step 1: Write helper**

```typescript
/**
 * Resolve a bilingual field based on current language.
 * When lang=en, prefer nameEn; fall back to name.
 * When lang=zh (or anything else), use name.
 */
export function localized(
  item: { name: string; nameEn?: string },
  lang: string,
): string {
  if (lang === 'en' && item.nameEn) return item.nameEn
  return item.name
}

export function localizedDesc(
  item: { description?: string; descriptionEn?: string },
  lang: string,
): string | undefined {
  if (lang === 'en') return item.descriptionEn || item.description
  return item.description
}
```

- [ ] **Step 2: Commit**

```bash
git add client/src/lib/i18n-utils.ts
git commit -m "feat: add localized() helper for bilingual field resolution"
```

---

## Chunk 2: Customer-side pages

### Task 6: Create language selection page

**Files:**
- Create: `client/src/pages/customer/LangSelectPage.tsx`
- Modify: `client/src/App.tsx`

- [ ] **Step 1: Create LangSelectPage**

```tsx
import { useParams, useNavigate } from 'react-router-dom'
import { useTranslation } from 'react-i18next'
import { Button } from '@/components/ui/button'

export default function LangSelectPage() {
  const { storeId, tableId } = useParams<{ storeId: string; tableId: string }>()
  const navigate = useNavigate()
  const { i18n } = useTranslation()

  const selectLang = (lang: string) => {
    i18n.changeLanguage(lang)
    localStorage.setItem('i18n-lang', lang)
    navigate(`/scan/${storeId}/${tableId}`, { replace: true })
  }

  return (
    <div className="flex flex-col items-center justify-center h-screen gap-8 p-4">
      <div className="text-center space-y-2">
        <h1 className="text-2xl font-bold">请选择语言</h1>
        <p className="text-lg text-muted-foreground">Please select your language</p>
      </div>
      <div className="flex flex-col gap-4 w-full max-w-xs">
        <Button size="lg" className="text-lg py-6" onClick={() => selectLang('zh')}>
          中文
        </Button>
        <Button size="lg" variant="outline" className="text-lg py-6" onClick={() => selectLang('en')}>
          English
        </Button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Add route in App.tsx**

Add import at top:
```typescript
import LangSelectPage from './pages/customer/LangSelectPage'
```

Add route before the ScanPage route:
```tsx
<Route path="/lang-select/:storeId/:tableId" element={<LangSelectPage />} />
```

- [ ] **Step 3: Update ScanPage to redirect to lang-select if no language set**

In `client/src/pages/customer/ScanPage.tsx`, in the `init()` function, before `session.setSession(...)`, add:

```typescript
// If no language preference set, redirect to language selection
const hasLang = localStorage.getItem('i18n-lang')
if (!hasLang) {
  navigate(`/lang-select/${storeId}/${tableId}`, { replace: true })
  return
}
```

- [ ] **Step 4: Commit**

```bash
git add client/src/pages/customer/LangSelectPage.tsx client/src/App.tsx client/src/pages/customer/ScanPage.tsx
git commit -m "feat: add language selection page for first-time customers"
```

---

### Task 7: i18n customer MenuPage

**Files:**
- Modify: `client/src/pages/customer/MenuPage.tsx`

- [ ] **Step 1: Add i18n imports and hooks**

Add at top:
```typescript
import { useTranslation } from 'react-i18next'
import { localized, localizedDesc } from '@/lib/i18n-utils'
```

Inside the component, add:
```typescript
const { t, i18n } = useTranslation('customer')
const lang = i18n.language
```

- [ ] **Step 2: Replace hardcoded Chinese strings**

Replace all hardcoded Chinese text with `t()` calls:
- `"搜索菜品..."` → `{t('menu.searchPlaceholder')}`
- `"未找到相关菜品"` → `{t('menu.noResults')}`
- `"选规格"` → `{t('menu.selectSpec')}`
- `"起"` → `{t('menu.from')}`
- `"必选"` → `{t('menu.required')}`
- `"加入购物车"` → `{t('menu.addToCart')}`
- `"去下单"` → `{t('menu.goOrder')}`
- `"查看订单"` → `{t('menu.viewOrders')}`
- `"Sold out"` → `{t('common:soldOut')}`
- `"Loading menu..."` → `{t('common:loading')}`

- [ ] **Step 3: Use localized() for menu item display**

Replace `{item.name}` with `{localized(item, lang)}` for menu item names.
Replace `{item.description}` with `{localizedDesc(item, lang)}` for descriptions.
Replace `{cat.name}` with `{localized(cat, lang)}` for category names.
Replace option/choice names with `{localized(opt, lang)}` and `{localized(choice, lang)}`.

- [ ] **Step 4: Add language toggle in header**

In the header section, add a small toggle button:
```tsx
<Button
  variant="ghost"
  size="sm"
  className="text-xs"
  onClick={() => {
    const next = lang === 'zh' ? 'en' : 'zh'
    i18n.changeLanguage(next)
    localStorage.setItem('i18n-lang', next)
  }}
>
  {t('common:langSwitch')}
</Button>
```

- [ ] **Step 5: Commit**

```bash
git add client/src/pages/customer/MenuPage.tsx
git commit -m "feat: i18n customer menu page with bilingual dish names"
```

---

### Task 8: i18n customer CartPage

**Files:**
- Modify: `client/src/pages/customer/CartPage.tsx`

- [ ] **Step 1: Add i18n imports**

```typescript
import { useTranslation } from 'react-i18next'
```

Inside component:
```typescript
const { t } = useTranslation('customer')
```

- [ ] **Step 2: Replace all hardcoded Chinese text**

Replace all Chinese strings with `t()` calls using the keys from `customer.json`:
- `"未找到桌台信息"` → `{t('cart.noTable')}`
- `"请扫描桌上的二维码开始点餐"` → `{t('cart.scanPrompt')}`
- `"购物车为空"` → `{t('cart.emptyCart')}`
- `"去菜单选点好吃的吧"` → `{t('cart.emptyPrompt')}`
- `"返回菜单"` → `{t('cart.backToMenu')}`
- `"我的订单"` → `{t('cart.title')}`
- `"桌号"` → `{t('cart.tableLabel')}`
- `"已点菜品（共 X 单）"` → `{t('cart.previousOrders', { count: previousOrders.length })}`
- `"已点合计"` → `{t('cart.previousTotal')}`
- `"本次加菜"` / `"当前购物车"` → `{hasPrevOrders ? t('cart.addMore') : t('cart.currentCart')}`
- `"/ 份"` → `{t('cart.perServing')}`
- `"添加备注（可选）"` → `{t('cart.remarkPlaceholder')}`
- `"提交订单"` / `"提交加菜"` → `{submitting ? t('cart.submitting') : hasPrevOrders ? t('cart.submitAddMore') : t('cart.submitOrder')}`
- `"继续点菜"` → `{t('cart.continueOrder')}`
- Status labels: use `t('common:status.pending')` etc.

- [ ] **Step 3: Commit**

```bash
git add client/src/pages/customer/CartPage.tsx
git commit -m "feat: i18n customer cart page"
```

---

### Task 9: i18n customer OrderConfirmPage

**Files:**
- Modify: `client/src/pages/customer/OrderConfirmPage.tsx`

- [ ] **Step 1: Add i18n and replace strings**

Add imports, use `t()` for all strings:
- `"未找到订单"` → `{t('orderConfirm.notFound')}`
- `"下单成功！"` → `{t('orderConfirm.success')}`
- `"订单号"` → `{t('orderConfirm.orderNumber')}`
- `"订单明细"` → `{t('orderConfirm.orderDetail')}`
- `"合计"` → `{t('common:total')}`
- `"继续点菜"` → `{t('orderConfirm.continueOrder')}`
- `"查看全部订单"` → `{t('orderConfirm.viewAllOrders')}`

- [ ] **Step 2: Commit**

```bash
git add client/src/pages/customer/OrderConfirmPage.tsx
git commit -m "feat: i18n customer order confirmation page"
```

---

## Chunk 3: Admin sidebar + admin pages

### Task 10: Collapsible admin sidebar with language toggle

**Files:**
- Modify: `client/src/components/AdminLayout.tsx`

- [ ] **Step 1: Add state and i18n imports**

```typescript
import { useState } from 'react'
import { useTranslation } from 'react-i18next'
import { ChevronLeft, ChevronRight, Globe, LogOut } from 'lucide-react'
```

Inside component:
```typescript
const { t, i18n } = useTranslation('admin')
const [collapsed, setCollapsed] = useState(() => localStorage.getItem('sidebar-collapsed') === '1')

const toggleCollapse = () => {
  const next = !collapsed
  setCollapsed(next)
  localStorage.setItem('sidebar-collapsed', next ? '1' : '0')
}

const toggleLang = () => {
  const next = i18n.language === 'zh' ? 'en' : 'zh'
  i18n.changeLanguage(next)
  localStorage.setItem('i18n-lang', next)
}
```

- [ ] **Step 2: Update NAV_ITEMS to use translation keys**

Change NAV_ITEMS to use i18n keys:
```typescript
const NAV_ITEMS = [
  { to: '/admin/dashboard', labelKey: 'nav.orders', icon: '📋' },
  { to: '/admin/menu', labelKey: 'nav.menu', icon: '🍜' },
  { to: '/admin/categories', labelKey: 'nav.categories', icon: '📂' },
  { to: '/admin/tables', labelKey: 'nav.tables', icon: '🪑' },
  { to: '/admin/settings', labelKey: 'nav.settings', icon: '⚙️' },
]
```

Update rendering to use `t(item.labelKey)` instead of `item.label`.

- [ ] **Step 3: Implement collapsible layout**

Change sidebar width: `className={collapsed ? 'w-16' : 'w-56'}` with `transition-all duration-200`.

Header: show full title when expanded, collapse button only when collapsed.

Nav items: show icon + text when expanded, icon only when collapsed.

- [ ] **Step 4: Add language toggle and collapse button**

Bottom of sidebar:
```tsx
{/* Language toggle */}
<button onClick={toggleLang} className="flex items-center gap-2 px-4 py-2 text-sm text-gray-600 hover:bg-gray-100 w-full">
  <Globe className="h-4 w-4 shrink-0" />
  {!collapsed && <span>{t('common:langSwitch')}</span>}
</button>

{/* Collapse toggle */}
<button onClick={toggleCollapse} className="flex items-center gap-2 px-4 py-2 text-sm text-gray-400 hover:bg-gray-100 w-full">
  {collapsed ? <ChevronRight className="h-4 w-4" /> : <ChevronLeft className="h-4 w-4" />}
  {!collapsed && <span>收起</span>}
</button>
```

User info section: show username when expanded, avatar icon when collapsed.

- [ ] **Step 5: Commit**

```bash
git add client/src/components/AdminLayout.tsx
git commit -m "feat: collapsible admin sidebar with language toggle"
```

---

### Task 11: i18n admin LoginPage

**Files:**
- Modify: `client/src/pages/admin/LoginPage.tsx`

- [ ] **Step 1: Add i18n and replace strings**

Add `useTranslation('admin')`, replace:
- `"后台登录"` → `{t('login.title')}`
- `"门店 ID"` → `{t('login.storeId')}`
- `"用户名"` → `{t('login.username')}`
- `"密码"` → `{t('login.password')}`
- `"登录"` / `"登录中..."` → `{submitting ? t('login.submitting') : t('login.submit')}`

- [ ] **Step 2: Commit**

```bash
git add client/src/pages/admin/LoginPage.tsx
git commit -m "feat: i18n admin login page"
```

---

### Task 12: i18n admin DashboardPage

**Files:**
- Modify: `client/src/pages/admin/DashboardPage.tsx`

- [ ] **Step 1: Add i18n and replace strings**

Add `useTranslation('admin')`, replace all Chinese strings with `t()` calls using `dashboard.*` and `common:status.*` keys.

- [ ] **Step 2: Commit**

```bash
git add client/src/pages/admin/DashboardPage.tsx
git commit -m "feat: i18n admin dashboard page"
```

---

### Task 13: i18n admin MenuManagePage + English name fields

**Files:**
- Modify: `client/src/pages/admin/MenuManagePage.tsx`

- [ ] **Step 1: Add i18n imports**

```typescript
import { useTranslation } from 'react-i18next'
```

- [ ] **Step 2: Replace all hardcoded Chinese with t() calls**

Use `menuManage.*` keys from admin namespace.

- [ ] **Step 3: Add nameEn input field in edit dialog**

After the `菜名` input, add:
```tsx
<div>
  <label className="text-sm font-medium">{t('menuManage.dishNameEn')}</label>
  <Input
    value={editingItem.nameEn ?? ''}
    onChange={e => updateEditingField('nameEn', e.target.value)}
    placeholder="Kung Pao Chicken"
  />
</div>
```

- [ ] **Step 4: Add descriptionEn textarea in edit dialog**

After the description textarea, add:
```tsx
<div>
  <label className="text-sm font-medium">{t('menuManage.descriptionEn')}</label>
  <Textarea
    value={editingItem.descriptionEn ?? ''}
    onChange={e => updateEditingField('descriptionEn', e.target.value)}
    placeholder="Classic Sichuan dish"
    rows={2}
  />
</div>
```

- [ ] **Step 5: Add nameEn fields to option/choice editors**

For each option name input, add a second input below for `nameEn`.
For each choice name input, add a second input below for `nameEn`.

- [ ] **Step 6: Pass nameEn through in handleSave**

Ensure `nameEn`, `descriptionEn` are included in the create/update API calls.

- [ ] **Step 7: Commit**

```bash
git add client/src/pages/admin/MenuManagePage.tsx
git commit -m "feat: i18n menu manage page with English name fields"
```

---

### Task 14: i18n admin CategoryManagePage + English name field

**Files:**
- Modify: `client/src/pages/admin/CategoryManagePage.tsx`

- [ ] **Step 1: Add i18n and replace strings**

Use `categoryManage.*` keys.

- [ ] **Step 2: Add nameEn input in category edit dialog**

After the category name input, add a `nameEn` field.

- [ ] **Step 3: Commit**

```bash
git add client/src/pages/admin/CategoryManagePage.tsx
git commit -m "feat: i18n category manage page with English name field"
```

---

### Task 15: i18n remaining admin pages

**Files:**
- Modify: `client/src/pages/admin/TablesPage.tsx`
- Modify: `client/src/pages/admin/StoreSettingsPage.tsx`
- Modify: `client/src/components/OrderDetailDialog.tsx`
- Modify: `client/src/components/OrderReceipt.tsx`

- [ ] **Step 1: i18n TablesPage**

Use `tables.*` keys.

- [ ] **Step 2: i18n StoreSettingsPage**

Use `storeSettings.*` keys.

- [ ] **Step 3: i18n OrderDetailDialog**

Use `orderDetail.*` keys.

- [ ] **Step 4: i18n OrderReceipt**

Use `receipt.*` keys.

- [ ] **Step 5: Commit**

```bash
git add client/src/pages/admin/TablesPage.tsx client/src/pages/admin/StoreSettingsPage.tsx client/src/components/OrderDetailDialog.tsx client/src/components/OrderReceipt.tsx
git commit -m "feat: i18n tables, settings, order detail, and receipt"
```

---

## Chunk 4: Server + seed data

### Task 16: Update server to pass through English fields

**Files:**
- Modify: `server/src/controllers/menu.controller.ts`

- [ ] **Step 1: Ensure new fields are passed through**

In the create and update handlers, include `nameEn`, `descriptionEn` in the data passed to the repository. Since the storage is JSON-based and uses spread, the new optional fields should pass through automatically. Verify this by reading the controller code.

- [ ] **Step 2: Commit (if changes needed)**

```bash
git add server/src/controllers/menu.controller.ts
git commit -m "feat: pass through English name fields in menu controller"
```

---

### Task 17: Add English names to seed data

**Files:**
- Modify: `server/src/seed.ts`

- [ ] **Step 1: Add nameEn to store 1 categories**

```typescript
const store1Categories = [
  { id: uuid(), storeId: store1Id, name: '热菜', nameEn: 'Hot Dishes', sortOrder: 1 },
  { id: uuid(), storeId: store1Id, name: '凉菜', nameEn: 'Cold Dishes', sortOrder: 2 },
  { id: uuid(), storeId: store1Id, name: '饮品', nameEn: 'Beverages', sortOrder: 3 },
  { id: uuid(), storeId: store1Id, name: '主食', nameEn: 'Staples', sortOrder: 4 },
]
```

- [ ] **Step 2: Add nameEn/descriptionEn to store 1 menu items**

Add `nameEn` and `descriptionEn` to each item. Examples:
- 宫保鸡丁 → `nameEn: 'Kung Pao Chicken'`, `descriptionEn: 'Classic Sichuan dish'`
- 麻婆豆腐 → `nameEn: 'Mapo Tofu'`, `descriptionEn: 'Spicy and numbing'`
- etc.

- [ ] **Step 3: Add nameEn to store 2 categories and items**

Same pattern for store 2 (火锅世界):
- 锅底 → `nameEn: 'Soup Base'`
- 肉类 → `nameEn: 'Meats'`
- 麻辣锅底 → `nameEn: 'Spicy Soup Base'`
- etc.

- [ ] **Step 4: Commit**

```bash
git add server/src/seed.ts
git commit -m "feat: add English names to seed data for both stores"
```

---

## Chunk 5: Verification

### Task 18: End-to-end verification

- [ ] **Step 1: Build client to check for TypeScript errors**

```bash
cd /Users/evergreen/Desktop/个人代码/QR_Code
pnpm --filter client build
```
Expected: no errors

- [ ] **Step 2: Start dev server and test customer flow**

1. Open `http://localhost:5173/scan/store-demo-001/<tableId>` — should redirect to language selection page
2. Select English — should redirect to menu page
3. Menu should show English names (where available)
4. Toggle language button in header — should switch to Chinese
5. Navigate to cart — should show English text
6. Refresh page — language should persist (localStorage)

- [ ] **Step 3: Test admin flow**

1. Open `http://localhost:5173/admin/login`
2. Login — sidebar should be collapsible
3. Click collapse button — sidebar shrinks to icons
4. Click globe icon — UI switches to English
5. Navigate to Menu Management — should see English name fields in edit dialog
6. Navigate to Category Management — should see English name field

- [ ] **Step 4: Final commit**

```bash
git add -A
git commit -m "feat: complete i18n implementation with zh/en support"
```
